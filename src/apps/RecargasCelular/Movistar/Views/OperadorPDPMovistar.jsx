import React from "react";
import HNavbar from "../../../../components/Base/HNavbar";

const OperadorPDPMovistar = ({ subRoutes }) => {
  return <HNavbar links={subRoutes} isIcon />;
};

export default OperadorPDPMovistar;
