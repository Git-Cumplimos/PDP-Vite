import React from "react";
import HNavbar from "../../components/Base/HNavbar/HNavbar";

const TelefoniaMovil = ({ subRoutes, permission }) => {
  return <HNavbar links={subRoutes} />;
};

export default TelefoniaMovil;
