import HNavbar from "../../../components/Base/HNavbar";

const CorresponsaliaBancoAgrario = ({ subRoutes }) => {
  return <HNavbar links={subRoutes} isIcon />;
};

export default CorresponsaliaBancoAgrario;
