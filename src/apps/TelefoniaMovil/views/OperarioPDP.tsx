import React from "react";
import HNavbar from "../../../components/Base/HNavbar/HNavbar";

const OperarioPDP = ({ subRoutes }: any) => {
  return <HNavbar links={subRoutes} />;
};

export default OperarioPDP;
